
import React from "react";
import { PageLayout } from "@/components/layout/PageLayout";
import { LeadFormContent } from "@/components/leadform/LeadFormContent";

const LeadForm = () => {
  return (
    <PageLayout>
      <LeadFormContent />
    </PageLayout>
  );
};

export default LeadForm;
